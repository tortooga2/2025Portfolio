const WIPContent = ({ link }) => {
  return (
    <div style={{ textAlign: "left", padding: "10px" }}>
      <p>WORK IN PROGRESS Can be found on my github <a href="{link}">@Here</a> </p>
    </div>
  );
};

export default WIPContent;
